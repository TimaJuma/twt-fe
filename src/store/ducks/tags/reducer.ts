import { LoadingState, TagsState } from "./contracts/state";

const initialState: TagsState = {
  items: [],
  loadingState: LoadingState.LOADING,
};

export const tagsReducer = (state: TagsState): void => {
  return;
};
